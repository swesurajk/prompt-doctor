{
  "name": "prompt-copilot",
  "version": "0.1.0",
  "private": true,
  "description": "Prompt Copilot — AI Prompt Analyzer & Assistant (Chrome MV3)",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "watch": "node tools/build.mjs --watch",
    "icons": "node tools/make-icons.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run test && npm run build",
    "eval": "node tools/eval.mjs",
    "zip": "npm run build && cd dist && zip -qr ../prompt-copilot.zip . && echo 'packaged → prompt-copilot.zip'"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.23.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
